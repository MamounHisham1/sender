- Prefers Expo Go for mobile app development (QR/hot-reload over Wi-Fi) over native Android APK/Gradle builds. Confidence: 0.9
- Prefers pkexec over sudo when running privileged commands. Confidence: 0.95
- User's laptop runs ufw actively (default-deny): when exposing dev servers to the LAN, add an explicit allow rule; loopback-based tests bypass ufw entirely, so passing local end-to-end tests doesn't prove remote devices can connect. Confidence: 0.9
- User's desktop is a Wayland (Hyprland) session: clipboard work must be verified with native tools, and `wl-paste` needs `-t <mime>` for non-text offers — bare `wl-paste` requests text and falsely reads empty against an image-only offer. Don't trust clipboard libraries' success returns either: arboard's `wayland-data-control` backend reported success while silently dropping large images. The proven-reliable pattern on this machine is writing a PNG temp file and running `setsid wl-copy -t image/png < file` detached (piping bytes into wl-copy from Rust proved racy); xclip stays the X11 fallback. XWayland clipboard writes remain invisible to Wayland apps. Confidence: 0.95
- Demands truthful success reporting: a ✓ in logs/UI must reflect the verified OS-level outcome — propagate real results back (reply channels, post-spawn liveness checks) instead of fire-and-forget claims; the user validates by checking actual live state (e.g., pastes the clipboard) and treats a mismatched success message as a bug. Confidence: 0.85
- When validating fixes, test with production-scale inputs: tiny synthetic fixtures kept passing while real ~12MP phone-photo payloads failed silently — large-payload code paths hide bugs small samples never reach. Confidence: 0.75
- Cares about runtime resource cost of running services: asks whether features are heavy on RAM and expects answers backed by actually measured numbers (e.g., ps RSS sampled idle / peak / settled) rather than guesses or reassurances. Confidence: 0.6
